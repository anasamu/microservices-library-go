package core

// Package core holds common shared types or utilities that are referenced by other
// modules in this monorepo. It's intentionally minimal to satisfy existing
// replace directives and imports like github.com/anasamu/microservices-library-go/core.

